name: GDCJ TrainDetail Runner

on:
  schedule:
    # 每天16点15分UTC运行（北京时间0点15分）
    - cron: '15 16 * * *'
  # 允许手动触发
  workflow_dispatch:

jobs:
  run-test:
    runs-on: ubuntu-latest
    
    # 添加权限设置
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # 添加token以允许推送更改
          token: ${{ secrets.GITHUB_TOKEN }}
          # 获取所有历史记录以便打tag
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # =======================================================
      # 👇 新增的验证步骤 (带超时) 👇
      # =======================================================
      - name: Verify Gitee Accessibility
        run: |
          echo "🔍 Verifying Gitee accessibility from the runner..."
          
          # 设置超时参数
          # --connect-timeout 10: 10秒内必须连接成功
          # -m 15: 整个请求在15秒内必须完成
          # -s: 静默模式
          # -o /dev/null: 丢弃响应体
          # -I: 只获取头部信息
          # -w "%{http_code}": 只输出HTTP状态码
          HTTP_STATUS=$(curl -s -o /dev/null -I -w "%{http_code}" --connect-timeout 10 -m 15 https://gitee.com)
          
          # 检查 curl 命令本身是否因为超时或网络错误而失败 (返回码非0)
          if [ $? -ne 0 ]; then
            echo "❌ Verification failed! curl command timed out or encountered a network error."
            echo "❌ This runner's IP is likely blocked or cannot reach Gitee reliably."
            exit 1
          fi
          
          # 检查 HTTP 状态码
          if [ "$HTTP_STATUS" -ne 200 ]; then
            echo "❌ Verification failed!"
            echo "❌ Gitee returned HTTP status: $HTTP_STATUS"
            echo "❌ This runner's IP is likely blocked by Gitee."
            exit 1
          else
            echo "✅ Verification successful! Gitee is accessible (HTTP 200)."
          fi
      # =======================================================

      - name: Run test script
        env:
          API_T: ${{ secrets.API_T }}
          API_D: ${{ secrets.API_D }}
          GITEE_TRAIN_LIST_URL: ${{ secrets.GITEE_TRAIN_LIST_URL }}
          GITEE_MINI_DATA_UPLOADER_URL: ${{ secrets.GITEE_MINI_DATA_UPLOADER_URL }}
          GITEE_MINI_DATA_DOWNLOADER_URL: ${{ secrets.GITEE_MINI_DATA_DOWNLOADER_URL }}
          GITEE_MINI_DATABASE_URL: ${{ secrets.GITEE_MINI_DATABASE_URL }}

          MY_GITHUB_TRAIN_LIST_URL: ${{ secrets.MY_GITHUB_TRAIN_LIST_URL }}
          MY_GITHUB_MINI_DATA_UPLOADER_URL: ${{ secrets.MY_GITHUB_MINI_DATA_UPLOADER_URL }}
          MY_GITHUB_MINI_DATA_DOWNLOADER_URL: ${{ secrets.MY_GITHUB_MINI_DATA_DOWNLOADER_URL }}
          MY_GITHUB_MINI_DATABASE_URL: ${{ secrets.MY_GITHUB_MINI_DATABASE_URL }}
        run: npx tsx scripts/processGDCJ.ts